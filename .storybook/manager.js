import { addons } from '@storybook/addons';
import { create } from '@storybook/theming';

addons.setConfig({
  theme: create({
    // base: 'dark',
    // brandImage: 'https://www.zxju.top/images/zg_films.png',
  }),
});
